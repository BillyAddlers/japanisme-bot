const { Collection } = require("discord.js");

module.exports = class TreeMap extends Collection {

    constructor() {
        super();
        this.waitformoreimplementation = "Wait for more implementation";
    }

};
